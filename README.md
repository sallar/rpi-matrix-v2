# Raspberry PI Matrix

![](matrix.png)

To install dependencies:

```bash
bun install
```

To build:

```bash
bunx run build
```

To sync:

```bash
bun run sync
```

## CAD Files

STEP and STL files are available in `cad` directory.
